version: 0.0
os: linux

files:
  - source: /my_app
    destination: /home/ec2-user/myapp

hooks:
  BeforeInstall:
    - location: scripts/stop.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/start.sh
      timeout: 300
      runas: root

